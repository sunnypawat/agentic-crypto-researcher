# Copy to `.env` for local development (do NOT commit real keys).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
CRYPTOPANIC_API_KEY=
TOOLS_CACHE_TTL_SECONDS=60
CORS_ALLOW_ORIGINS=*

# Frontend
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000 # or NEXT_PUBLIC_BACKEND_URL

# Token used to allow frontend to access backend for api protection
# Note is the same as .env
BACKEND_SHARED_SECRET=